# CLAUDE.md

## プロジェクト概要
個人発・公開型の「雑学・知識データベースWebアプリ」。
自分で調べた雑学や知識を登録・整理し、キーワードやカテゴリ（タグ）で
検索・閲覧できる知識辞典。自分は読み書きでき、他人は読むことができる。
PC・スマホ両方から同じURLで使える。
（アプリ名：未決定。フォルダ名・プロジェクト名は trivia-app）

## ターゲットユーザー
- 主：開発者本人（情報系の大学生）＝唯一の書き込み者
- 副：閲覧者（他人。読むことのみ可能）
- 開発者のスキル：プログラミングはほぼ初心者

## 解決する課題
新たな知識・雑学を増やしたいが「探す・理解する・続ける」がめんどうで定着しない。
→ 今回のスコープは「自分で調べた知識を散らからず集めて整理し、後から探せる
   状態にし、必要なら他人にも見せられる」ことに集中。
   （きっかけ作り＝再表示・通知は将来の課題として保留）

## 主要機能
- 【登録】タイトル → 本文 の順で入力して知識を追加
- 【本文2分割】AIの説明（貼り付け）と 自分のまとめ（自分の言葉）を分けて保存
  - 少なくともどちらか一方が入力されていれば登録可
- 【タグ分類】タグで分類。複数付与可・0個可・後から編集可
- 【検索・閲覧】キーワード検索＋タグ絞り込み（併用可）
- 【閲覧】詳細で本文・出典・タグを表示
- 【編集・削除】後から修正・削除（CRUD）
- 【権限】自分＝読み書き／他人＝読み取りのみ

## データ構造（knowledgeテーブル）
- id：int8・主キー・自動採番（Is Identity）
- created_at：timestamptz・now()（自動）
- title：text（必須）
- ai_explanation：text（任意）　┐ どちらか一方は必須（アプリ側でチェック）
- my_summary：text（任意）　　 ┘
- source：text（任意）
- tags：text[]（配列・任意・複数可）

## 技術スタック
- プラットフォーム：Webアプリ（PC/スマホ両対応）
- フロントエンド：HTML / CSS / JavaScript（素のJSで開始。将来TypeScriptへ移行）
- データ保存／認証：Supabase（PostgreSQL + Auth + RLS）
  - 接続情報は config.js に分離し、.gitignore で除外（公開キーのみ使用）
  - 秘密キー（service_role / sb_secret_）はブラウザ側で絶対に使わない
  - 注意：無料枠は7日間アクセスがないと一時停止（週1利用で回避可）
- ホスティング：Vercel（または Netlify）。無料枠で公開（段階3で実施）

## セキュリティ設定（実施済み）
- RLS有効。ポリシー2つを設定済み：
  - 「Enable read access for all users」：SELECT / public（誰でも読み取り可）
  - 「Allow authenticated write」：ALL / authenticated（ログイン者のみ書き込み可）
- 認証：メール＋パスワード方式。自分のユーザーはSupabase管理画面で作成済み
- Data API：public スキーマ公開、knowledge テーブルを個別に公開済み

## 開発計画（優先順位）
- 方針：ウォーキングスケルトン（一番細い一本を先に通す）
- 段階1（土台）：
  1. ✅ Supabase準備（テーブル・接続）
  2. ✅ 自分のログイン
  3. ✅ アプリからの接続確認
  4. ✅ ログイン → 1件保存 → 一覧表示（ローカルで動作確認済み）
  ※ 別端末での閲覧は段階3（Vercel公開）で実現する
- 段階2：知識アプリとして使える形に
  5. 編集・削除（CRUD完成）
  6. タグ機能の拡充
  7. キーワード検索＋タグ絞り込み
- 段階3：公開・仕上げ
  8. 見た目を整える（スマホ表示調整）
  9. Vercelで公開しURLを共有 → 別端末確認

## 現在の優先タスク
- 段階3へ進む：見た目を整える → Vercelで公開 → 別端末で確認

## コーディング規約
（必要に応じて後で定める。素のJSのうちは最小限でよい）

## 決定済み事項
- ✅ アプリの核は「自分で集めた雑学・知識を整理し、後から探せること」
- ✅ 権限設計：自分＝読み書き／他人＝読み取りのみ（公開型）
- ✅ ログイン認証は必須（Supabase認証＋RLS）。設定済み
- ✅ 登録の入力順は「タイトル → 本文」
- ✅ 本文は AIの説明 と 自分のまとめ の2欄に分ける
- ✅ 登録条件：title必須＋本文2欄のうち少なくとも一方が入力されていること
- ✅ タグは複数付与可・0個可・後から変更可
- ✅ 検索はキーワード検索＋タグ絞り込みの両方（併用可）
- ✅ 画面は3つ（一覧・詳細・登録/編集）。削除は確認のうえ実行
- ✅ 再表示・通知などの受動的な仕組みは作らない（能動型に一本化）
- ✅ プラットフォームはWebアプリ（PC・スマホ両対応）
- ✅ データはSupabaseに保存し、端末をまたいで共有
- ✅ フロントは素のHTML/CSS/JSで開始（最短完成優先）、将来TypeScript移行
- ✅ ホスティングはVercel（またはNetlify）、全て無料枠
- ✅ 雑学の提供元は方式C：まず自分で登録、将来AI候補出し（最終確認は自分）を追加可能に
- ✅ 接続情報は config.js に分離、.gitignore で除外、公開キーのみ使用
- ✅ ウォーキングスケルトン（接続→ログイン→保存→表示）が通った
- ✅ source / ai_explanation / my_summary / tags を Nullable（任意）に修正済み（設計どおり）
- ✅ CRUD完成（登録・表示・編集・削除がアプリ上で動作）
- ✅ 検索機能完成（キーワード検索＋タグ絞り込み＋併用、未ログインでも閲覧・検索可）

## 未決定事項
- 🔲 アプリ名
- 🔲 「カテゴリ」と「タグ」どちらの呼び方にするか
- 🔲 各画面の細かいレイアウト・デザイン
- 🔲 コーディング規約の詳細
- 🔲 （将来）TypeScript移行のタイミング
- 🔲 （将来）AIによる雑学候補出し機能の追加（方式B）
- 🔲 （軽微）検索キーワードにカンマや括弧などの記号を含むと正しく動かない場合がある。通常の単語検索は問題なし。必要になったら対策する